{
  "name": "utzilcare-voice",
  "version": "0.1.0",
  "private": true,
  "description": "ElevenLabs + UtzilCare — voice interface for post-operative care instructions",
  "repository": {
    "type": "git",
    "url": "https://github.com/marcebd/utzilCare-voice.git"
  },
  "license": "MIT",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
